<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="20dp">

    <ImageView
        android:id="@+id/expenditure_avatar"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/avatar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/expenditure_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:text="Nazanin Nasab"
        android:textAllCaps="true"
        android:textColor="@color/navy_dark"
        android:textSize="18dp"
        app:layout_constraintStart_toEndOf="@+id/expenditure_avatar"
        app:layout_constraintTop_toBottomOf="@+id/expenditure_title"
        app:layout_constraintTop_toTopOf="@+id/expenditure_avatar" />

    <TextView
        android:id="@+id/expenditure_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:text="Buy stuff from store"
        android:textColor="@color/navy_light"
        android:textSize="14dp"
        app:layout_constraintBottom_toBottomOf="@+id/expenditure_avatar"
        app:layout_constraintStart_toEndOf="@+id/expenditure_avatar"
        app:layout_constraintTop_toBottomOf="@+id/expenditure_name" />

    <TextView
        android:id="@+id/expenditure_paid"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="+$350"
        android:textColor="@color/sharp_green"
        android:textSize="24dp"
        app:layout_constraintBottom_toBottomOf="@id/expenditure_title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/expenditure_name" />
</androidx.constraintlayout.widget.ConstraintLayout>